const INCREMENT = "INCREMENT";
const DECREMENT = "DECREMENT";

const incrementCounter = {
    type: INCREMENT
}

const decrementCounter = {
    type: DECREMENT
}

export {
    INCREMENT,
    DECREMENT,
    incrementCounter,
    decrementCounter
}
